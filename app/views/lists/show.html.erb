<div class="container">
  <h2><%= @list.name %></h2>
  <%# <p>There are <%= @list.movies.count %> movies in this list</p>

  <ul>
  <% @list.bookmarks.each do |bookmark| %>
  <li>
  <%= image_tag bookmark.movie.poster_url, alt: bookmark.movie.title , width: 100 %>
  <%= bookmark.movie.title %> - <%= bookmark.comment %> <%= link_to "❌", bookmark_path(bookmark), data: { turbo_method: :delete } %>
  <%= bookmark.movie.title %>
  <%= bookmark.movie.title %>
  </li>


  <%end%>
  </ul>


  <%= link_to "Add a Movie", new_list_bookmark_path(@list), class:"btn btn-primary" %>

  <p> <%= link_to "Back To List", lists_path, class:"btn btn-primary" %></p>


</div>
